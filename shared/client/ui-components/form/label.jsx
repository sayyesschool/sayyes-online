import { FormLabel as FluentFormLabel } from '@fluentui/react-northstar';

export default function FormLabel({ ...props }) {
    return (
        <FluentFormLabel
            {...props}
        />
    );
}