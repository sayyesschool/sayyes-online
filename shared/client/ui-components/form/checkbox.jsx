import { FormCheckbox as FluentFormCheckbox } from '@fluentui/react-northstar';

export default function FormCheckbox({ ...props }) {
    return (
        <FluentFormCheckbox
            {...props}
        />
    );
}