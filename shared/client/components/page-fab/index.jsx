import { FAB } from 'mdc-react';

import './index.scss';

export default function PageFAB(props) {
    return (
        <FAB
            className="page-fab"
            {...props}
        />
    );
}