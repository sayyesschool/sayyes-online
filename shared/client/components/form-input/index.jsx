import { FormInput as FluentFormInput } from '@fluentui/react-northstar';

export default function FormInput({ ...props }) {
    return (
        <FluentFormInput
            {...props}
        />
    );
}