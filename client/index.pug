extends /server/layouts/app

include /server/mixins/yandex-metrika
include /server/mixins/google-analytics

append head
    link(rel="stylesheet" href="/build/client.vendors.css")
    link(rel="stylesheet" href="/build/client.main.css")

append body
    //include /shared/views/includes/footer

    if ENV === 'production'
        +yandex-metrika(YANDEX_METRIKA_ID)(defer)
        +google-analytics(GOOGLE_ANALYTICS_ID)
        
    script(src="/build/client.vendors.js")
    script(src="/build/client.main.js")