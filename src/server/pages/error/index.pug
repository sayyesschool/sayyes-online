extends /shared/views/layouts/page

block main
    main#content
        +mdc-typography('Ой, произошла ошибка')(element='h1' type='headline4')

        +mdc-typography(message)(element='h1' type='headline6')

        if error
            +mdc-typography(error.message)(type='headline6')

            if IS_DEVELOPMENT
                pre= error.stack
        
        +mdc-typography('Если это уже не первый раз, напишите пожалуйста в службу поддержки.')(type='subtitle1')

        +mdc-fab('bug_report', 'Сообщить об ошибке')(element='a' href='mailto:support@sayes.ru?subject=Ошибка%20на%20сайте')