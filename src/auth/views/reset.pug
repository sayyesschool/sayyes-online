extends ./layout

block main
    +logo('english-school_purple')
    
    h1.heading.heading-5 Восстановление пароля

    +form#reset-form(method='post' action=`/reset/${token}`)
        input.input(
            id="password-input"
            type="password"
            name="password"
            minlength="6"
            maxlength="128"
            placeholder="Новый пароль"
            required
        )
        
        button.btn.btn--purple(type="submit") Сохранить

        a.link.link--interactive(href='/') Вернуться на страницу входа